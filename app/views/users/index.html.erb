<% provide :title, t(".index") %>
<h1><%= t ".index" %></h1>

<%= will_paginate @users, 
  list_classes: %w(pagination justify-content-center) %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate @users, 
  list_classes: %w(pagination justify-content-center) %>
