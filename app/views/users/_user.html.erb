<li>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user.current_user?(user) %>
    <%= link_to t("users._user.delete"), user, method: :delete,
      data: {confirm: t("users._user.sure")} %>
  <% end %>
</li>
